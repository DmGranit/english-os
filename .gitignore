# данные и генерируемое (не версионируем)
*.db
backups/
bot_state.pickle
__pycache__/
*.pyc

# ручные бэкапы и архивы
*.bak*
*.zip
archive/

# секреты / окружение
.env
*.key
secrets*

# тесты
.pytest_cache/
.pytest_tmp/

# одноразовый осмотр/отладка — не возвращать в репо
.live_check.db
_inspect_*.py

# прочее
*.log

# локальная оснастка Claude Code — НЕ коммитится:
# junction-скил session-capture (SSOT в C:\claude-tooling), профиль и
# операционный маркер last_capture_ref (HEAD прошлого захвата) — всё локальное состояние
.claude/
