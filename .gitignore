# данные и генерируемое (не версионируем)
*.db
backups/
bot_state.pickle
__pycache__/
*.pyc

# ручные бэкапы и архивы
*.bak*
*.zip
archive/

# секреты / окружение
.env
*.key
secrets*

# тесты
.pytest_cache/
.pytest_tmp/

# одноразовый осмотр/отладка — не возвращать в репо
.live_check.db
_inspect_*.py

# прочее
*.log

# операционный маркер захвата сессии (локальное состояние, не коммитится)
.claude/last_capture_ref
