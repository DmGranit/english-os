# English OS — список улучшений (бот + процесс)

> **Источник:** чекап от 2026-06-09 — сверка наработок MVP против предыдущего анализа и канона методики.
> **Приоритет:** P0 срочно · P1 важно · P2 потом · **Объём:** S мелочь · M средне · L крупно.

---

## A. Бот — улучшения продукта и кода

| # | Что | Зачем | P | Объём |
|---|---|---|---|---|
| A1 | **Живой тест дисциплины режимов** (SCENARIO держит роль, FLOW без «Switching…», `💬 Natural` к месту) | Правки в промпте и модели сделаны, но не подтверждены прогоном. Без проверки — не знаем, закрыт ли баг | P1 | S |
| A2 | **Регистр-проход по базе** (165/196 = 84% `neutral`) — батч-разметка formal/neutral/informal | Деловой английский живёт на регистре (email vs Slack). Слой register сейчас почти пустой | P1 | M |
| A3 | **Тесты**: SRS-переходы (`review`, коробки, направление), парсинг `ИТОГ`-контракта, `apply_session_summary` | Система выросла вдвое; цена регрессии растёт, а проверка только ручная | P1 | M |
| A4 | **QA-шаг в `enrich`** (мульти-ИИ верификация перед `/pending`) | Это **опора 4** позиционирования («стерео»), а в коде её нет — заявление повисло | P1 | M |
| A5 | **`placement_test` — решить судьбу**: подключить как объективный placement ИЛИ убрать | Полноценный код висит мёртвым; онбординг ушёл в самовыбор темпа | P2 | S |
| A6 | **Расширить регекс намерения** («I'd like to learn X», «давай разберём X») | Сейчас не ловит ровно ту фразу, что писал Dmitry в логе | P2 | S |
| A7 | **Whisper: языковая подсказка** (`language` ru/en) + чистка | В первом логе транскрипция галлюцинировала; подсказка резко стабилизирует | P2 | S |
| A8 | **Персистентность** режима/истории (`PicklePersistence`) | Рестарт бота теряет режим и контекст диалога | P2 | S |
| A9 | **`match_words`**: не инжектить сверхчастые слова, мягкий приоритет по `priority` | Возможна переинъекция шумных совпадений (cap=4 смягчает, но грубо) | P2 | S |
| A10 | **Уборка**: `english_os.json.bak/bak2/bak3/bak4`, `*.zip` в архив | Плодятся вручную, путают рабочее с архивом | P2 | S |

---

## B. Процесс — как мы работаем

| # | Что | Зачем | P | Объём |
|---|---|---|---|---|
| B1 | **Поставить git** (+`.gitignore` на `*.db`, `backups/`, `*.bak*`, `*.zip`, секреты) | Проект созрел, а версионируется руками через `.bak2/3/4`. Git убирает класс проблем и даёт нормальный diff между сессиями | P0 | S |
| B2 | **Ритуал «чекап» после каждого батча соседа** | Сегодня код ушёл вперёд канона за один заход. Без сверки канон-как-source-of-truth разойдётся с кодом | P1 | S |
| B3 | **Формализовать «стерео»**: кто что ведёт (сессия-методика ↔ сессия-MVP) и где точки сверки | Чтобы расхождения ловились системно (как сегодня — 2 опоры позиционирования, которых не было в каноне), а не случайно | P1 | S |
| B4 | **Decision log** (журнал осознанных решений): BEC снят, самовыбор vs placement, «сеть = архитектура» | Чтобы не переспоривать по кругу уже принятое; для интенсива — готовая «история решений» | P1 | S |
| B5 | **Канон → нарезка артефактов**: из одного source of truth — студенческое описание для интенсива + внешний манифест | Один источник, разные аудитории, без рассинхрона | P2 | M |
| B6 | **План сбора данных под A/B** (сеть vs плоско): сейчас n=1; нужна когорта и протокол | «Сетевой буст» останется ⏳ навсегда без данных. Заодно — то, что реально можно показать инвестору | P2 | L |
| B7 | **Регресс-набор из реальных логов** (Dmitry) как чек-лист перед каждым релизом промпта | Логи уже ловят то, что код не показывает (режимы, голос). Сделать это повторяемым | P2 | S |

---

## Особые акценты

- **B1 (git) стоит поднять в P0** — это страховка под всё остальное и под параллельную работу двух сессий.
  Сейчас откат к рабочей версии = «надеюсь, нужный `.bak`».
- **A4 + B3 связаны:** «мульти-ИИ стерео» — одновременно заявленная опора продукта и наш фактический метод работы.
  Довести её до реализованной → перестанет быть ⏳ и станет настоящим отличием.

---

## Статус прошлых находок (для контекста)

Закрыто соседом в этом батче: контент-граф в свободном диалоге · блокирующий I/O · дисциплина режимов
(в промпте) · docstring-нит · текст `on_unsupported`. Добавлено сверх списка: онбординг по темпу +
адаптивная полоса · `learner_profile` · `💬 Natural` · темы · «🔍 Глубже» · напоминания · меню команд.
Осталось открытым: регистр базы · тесты · Whisper-язык · персистентность.
